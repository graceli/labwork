working on painstakingly dissecting each parameter for the heme molecule
I've finished identifying all the unique bond types so far
Next is double checking that for each bond under a particular bond type if the parameters are the same
